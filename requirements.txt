# Pinned dependencies as used at paper submission.
# Lower bounds reflect the versions used; upper bounds left open to allow
# newer Python versions (numpy 2.x is required for Python 3.13+).

# Core data handling
pandas>=2.1,<3.0
numpy>=1.26,<3.0

# Database
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9

# HTTP / xDD API
requests>=2.31

# NLP
nltk>=3.8

# Environment loading
python-dotenv>=1.0

# Optional: development / testing
# pytest~=7.4
# jupyter~=1.0